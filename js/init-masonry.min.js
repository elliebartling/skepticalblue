(function($) {

	//jQuery
	$( document ).ready(function() {
		$('#container').masonry({
		  itemSelector: '.brick',
		  transitionDuration: '0.8s'
		});
	});

	$( document ).ready(function() {
		$('.wp_rss_retriever_list').masonry({
		  itemSelector: '.wp_rss_retriever_item',
		  transitionDuration: '0.8s',
		  gutter : 20
		});
	});

})(jQuery);
